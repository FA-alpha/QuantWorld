version: '3.8'

services:
  quantworld:
    build: .
    ports:
      - "8000:8000"
    environment:
      - COINGLASS_API_KEY=${COINGLASS_API_KEY}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
